{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run FastAPI server",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app.main:app",
                "--reload"
            ],
            "cwd": "${workspaceFolder}\\src"
        }
    ]
}